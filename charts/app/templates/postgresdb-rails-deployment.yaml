apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: postgres-rails
  name: postgres-rails
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-rails
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: postgres-rails
    spec:
      containers:
        - image: postgres
          image: "{{ .Values.postgres.image.repository }}:{{ .Values.postgres.image.tag }}"
          name: postgres-rails
          imagePullPolicy: {{ .Values.postgres.image.pullPolicy }}
          ports:
            - containerPort: 5433
          resources: {}
          volumeMounts:
            - mountPath: /data/db
              name: postgres-rails-0
      restartPolicy: Always
      volumes:
        - name: postgres-rails-0
          persistentVolumeClaim:
            claimName: postgres-rails-0
